<?xml version="1.0" encoding="utf-8"?>
<base:BaseContainer xmlns:fx="http://ns.adobe.com/mxml/2009" 
                    xmlns:s="library://ns.adobe.com/flex/spark" 
                    xmlns:mx="library://ns.adobe.com/flex/mx"
                    xmlns:base="components.base.*"
                    xmlns:ctrl="components.map.controllers.*"
                    xmlns:minimap="components.map.controllers.minimap.*"
                    implements="components.map.controllers.IMapViewportController"
                    initialize="this_initializeHandler(event)">
   
   <fx:Script>
      <![CDATA[
         import components.base.viewport.Viewport;
         
         import models.MStaticSpaceObjectsAggregator;
         
         import mx.events.FlexEvent;
         
         import utils.datastructures.Collections;
         
         
         private var _viewport:Viewport;
         public function setViewport(viewport:Viewport) : void
         {
            _viewport = viewport;
            initializeControllers();
         }
         
         
         public function cleanup() : void
         {
            _viewport = null;
            zoom.cleanup();
         }

         
         private function this_initializeHandler(event:FlexEvent):void
         {
            initializeControllers();
         }
         
         
         private function initializeControllers() : void
         {
            if (zoom)
            {
               zoom.setViewport(_viewport);
            }
            if (solarSystems)
            {
               solarSystems.dataProvider = ML.latestGalaxy.solarSystems;
            }
         }
      ]]>
   </fx:Script>
   
   <ctrl:ViewportZoomController id="zoom"/>
   <ctrl:MapObjectsList id="solarSystems" right="2" bottom="2" height="40%"
                        itemRenderer="components.skins.itemrenderers.IRSolarSystem"/>
   
</base:BaseContainer>
