package namespaces
{
   /**
    * Namespace for properties and methods in client code which should not be available in public
    * class interface but still we could access them if needed in tests, subclasses and so on.
    */
   public namespace client_internal;
}