package namespaces
{
   /**
    * Property change flags are defined in this namespace. They have the same name as the properties in the
    * public namespace and are all of <code>Boolean</code> type. If such flag is set to <code>true</code>
    * it means that a view will be updated on the next screen render. After that the flag is set to
    * <code>false</code> again. All flags should default to <code>true</code>.
    */
   public namespace change_flag;
}