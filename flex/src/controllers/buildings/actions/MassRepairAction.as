package controllers.buildings.actions
{

   import controllers.CommunicationAction;

   /**
    * Used for repairing all buildings in a planet, takes two params:
    * planetId: int and buildingIds: Array
    */
   public class MassRepairAction extends CommunicationAction
   {
   }
}